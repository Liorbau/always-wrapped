# Copy to .env and fill in. Never commit .env.

# Spotify app (developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8888/callback

# Any Postgres works — Supabase, Neon, RDS, a local server. Omit for SQLite.
DATABASE_URL=postgresql://user:password@host:5432/postgres
# SQLITE_PATH=my_spotify_data.db   # only used when DATABASE_URL is unset

# LLM provider for the agents (default: openai)
LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o            # optional override
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=...       # if LLM_PROVIDER=anthropic
# GOOGLE_API_KEY=...          # if LLM_PROVIDER=google
# ROUTER_MODEL=gpt-4o-mini    # optional cheaper model for chat routing
#
# openai / anthropic / google ship a default model. Any other LiteLLM provider
# also works — name it and give an explicit model:
#   LLM_PROVIDER=groq   LLM_MODEL=llama-3.3-70b-versatile   GROQ_API_KEY=...
#   LLM_PROVIDER=ollama LLM_MODEL=llama3

# Where proposals are pushed for approval: telegram (default) or none.
# "none" keeps approval in the app only; it never widens what agents may do.
# NOTIFIER=telegram

# Agent spend cap per day in USD (default 20.00)
# AGENT_DAILY_BUDGET_USD=20.00

# Deployed push token (contents of .runtime/spotify-push.cache) so headless hosts
# can push approved playlists without a browser login.
# SPOTIFY_PUSH_CACHE_CONTENT=

# Planner agent (calendar -> playlists -> Telegram approval). All optional;
# the Planner is disabled until CALENDAR_ICS_URL + the Telegram vars are set.
# Private .ics feed from Google/Apple Calendar (Settings -> secret address).
# CALENDAR_ICS_URL=https://calendar.google.com/calendar/ical/.../basic.ics
# Telegram bot from @BotFather; chat id from @userinfobot (your own DM).
# TELEGRAM_BOT_TOKEN=123456:ABC...
# TELEGRAM_CHAT_ID=123456789
# Shared secret Telegram echoes back on every webhook call (any random string).
# TELEGRAM_WEBHOOK_SECRET=some_long_random_string
# Timezone for /timer schedules (default Asia/Jerusalem).
# USER_TZ=Asia/Jerusalem
# Public base URL, used once by scripts/setup_telegram.py to register the webhook.
# APP_BASE_URL=https://your-app.example.com
# The nightly Planner is off until you schedule it over Telegram: /plantime HH:MM

# Deploy contract, for any host:
#   start command : python server.py   (or the bundled Dockerfile)
#   listens on    : $PORT (default 5000), bound to 0.0.0.0
#   writable dir  : .runtime/ (OAuth caches, agent trajectories)
#   RUNTIME_DIR=.runtime
#   PORT=5000

