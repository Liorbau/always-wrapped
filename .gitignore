# Secrets
.env

# Everything the app writes at runtime: OAuth token caches, agent run
# trajectories. Regenerable or externally backed — see core/paths.py.
.runtime/

# Local database
my_spotify_data.db

# Python
venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Claude Code local settings (personal, not shared)
.claude/settings.local.json
.claude/worktrees/

# Fellowship workshop slides (reference only, not shipped)
*.pdf

# Local dead-code scanners — analysis tools, not part of the app
scripts/find_dead_code.py
scripts/find_dead_frontend.mjs
