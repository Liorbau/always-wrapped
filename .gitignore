# Security Keys
.env
.cache

# Database
my_spotify_data.db

# Python Junk
venv/
__pycache__/
*.pyc

# OS Junk
.DS_Store

# Debug Scripts
check_db.py
# Claude Code local settings (personal, not shared)
.claude/settings.local.json
.cache-push

# runtime artifacts (run logs, push/rejection state) — evidence is curated into the submission folder, not tracked here
agent-runs/

# Fellowship workshop slides (reference only, not shipped)
*.pdf

# Evidence/submission folder — local package (holds a ~700MB screen recording
# + curated run logs). Never committed; travels with the submission, not the repo.
evidence/
