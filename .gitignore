# Security Keys
.env
.cache

# Database
my_spotify_data.db

# Python Junk
venv/
__pycache__/
*.pyc

# OS Junk
.DS_Store

# Debug Scripts
check_db.py
# Claude Code local settings (personal, not shared)
.claude/settings.local.json
.cache-push

# runtime artifacts (run logs, push/rejection state) — evidence is curated into the submission folder, not tracked here
agent-runs/

# Fellowship workshop slides (reference only, not shipped)
*.pdf

# Evidence: track the curated text evidence (run logs, summaries) so it's visible
# in the repo; never commit the ~700MB screen recording.
evidence/*.mov
